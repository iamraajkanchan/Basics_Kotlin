package self.collections.treeSet

/**
 * Quality: Unique + Sorted
 * When to use: Automatically sort unique items.
 * Scenario: Train numbers should be unique and sorted.
 * Why use it?: Duplicates removed and elements are sorted.
 * */
class BasicTreeSetExamples {
}