package self.collections.arrays

/**
 * Quality: Fixed Size
 * When to use: Fixed-size data. The number of elements is known beforehand.
 * Scenario: A metro station has exactly 4 gates.
 * Why use it?: Fast access by index and less memory overhead.
 * */
class BasicArrayExamples {
}