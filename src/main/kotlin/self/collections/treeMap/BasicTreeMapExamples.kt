package self.collections.treeMap

/**
 * Quality: Sorted Keys
 * When to use: Key should remain sorted.
 * Scenario: Stations by station code should be in order.
 * Why use it?: Keys are sorted automatically.
 * */
class BasicTreeMapExamples {
}